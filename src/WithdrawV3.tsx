import {useEffect,useMemo,useState} from 'react';
import {TonConnectUI} from '@tonconnect/ui';
import {date,getInitData,money,PUBLISHABLE_KEY,SUPABASE_URL,type Snapshot,withdrawApi} from './lib';

type Method={method_key:string;label:string;network:'TON'|'Polygon';enabled:boolean;minimum_usdt:number;fee_usdt:number;sort_order:number};
type Step='method'|'amount'|'wallet'|'review';
const TON_ICON='https://pixlinkhost.vercel.app/i/1AcOzN0LcQ';
const USDT_ICON='https://pixlinkhost.vercel.app/i/DtKRX3jFfg';
const MANIFEST='https://wiener-farm.vercel.app/tonconnect-manifest.json';
let tonUI:TonConnectUI|null=null;
const getTonUI=()=>tonUI||(tonUI=new TonConnectUI({manifestUrl:MANIFEST}));
const short=(w:string)=>w.length<20?w:`${w.slice(0,8)}…${w.slice(-6)}`;
const floor4=(v:number)=>Math.floor(Math.max(0,v)*10000+1e-9)/10000;
const statusView=(s:string)=>s==='paid'?{icon:'🟢',label:'PAID'}:s==='rejected'?{icon:'🔴',label:'REJECTED'}:{icon:'🟡',label:'PENDING'};
function remaining(until:string|null){if(!until)return 0;return Math.max(0,new Date(until).getTime()-Date.now())}
function formatLeft(ms:number){const t=Math.ceil(ms/1000),h=Math.floor(t/3600),m=Math.floor((t%3600)/60),s=t%60;return h?`${h}h ${m}m ${s}s`:m?`${m}m ${s}s`:`${s}s`}
async function tonApi(action:string,body:any={}){const r=await fetch(`${SUPABASE_URL}/functions/v1/wiener-ton-wallet`,{method:'POST',headers:{'Content-Type':'application/json','apikey':PUBLISHABLE_KEY},body:JSON.stringify({action,initData:getInitData(),...body})});const x=await r.json().catch(()=>({ok:false,error:'invalid_response'}));if(!r.ok||!x.ok)throw new Error(x.message||x.error||'TON wallet request failed');return x.data??x}

export function WalletV2({data}:{data:Snapshot;setTab:any}){
 const s=data.settings;
 const preloaded=Array.isArray(data.withdrawal_methods)?data.withdrawal_methods as Method[]:[];
 const [methods,setMethods]=useState<Method[]>(preloaded),[step,setStep]=useState<Step>('method'),[methodKey,setMethodKey]=useState(''),[amount,setAmount]=useState(''),[wallet,setWallet]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[history,setHistory]=useState<any[]>(data.withdrawals||[]),[selected,setSelected]=useState<any>(null),[balance,setBalance]=useState(Number(data.user.balance||0)),[cooldownUntil,setCooldownUntil]=useState<string|null>(null),[cooldownMs,setCooldownMs]=useState(0),[hasPending,setHasPending]=useState((data.withdrawals||[]).some((w:any)=>String(w.status)==='pending')),[globalEnabled,setGlobalEnabled]=useState(!!s.withdrawals_enabled),[tonWallet,setTonWallet]=useState(''),[tonProvider,setTonProvider]=useState(''),[tonRate,setTonRate]=useState<number|null>(null),[tonBusy,setTonBusy]=useState(false);
 const method=methods.find(x=>x.method_key===methodKey),isGram=methodKey==='gram_ton',unit=isGram?'GRAM':'USDT',baseRate=Number(s.token_per_usdt||15000),rate=isGram?Number(tonRate||0):baseRate,gross=Number(amount||0),configuredFee=Number(method?.fee_usdt||0),fee=isGram?configuredFee:(gross>0&&gross<0.05?0:configuredFee),receive=Math.max(0,gross-fee),needed=gross*rate,available=rate>0?balance/rate:0,maxWithdrawable=floor4(available);
 const load=async(showError=false)=>{try{const [m,h,t]=await Promise.all([withdrawApi('methods'),withdrawApi('history'),tonApi('status')]);if(Array.isArray(m.methods))setMethods(m.methods);setBalance(Number(m.balance??balance));setCooldownUntil(m.cooldown_until||null);setCooldownMs(remaining(m.cooldown_until||null));setHasPending(!!m.has_pending);setGlobalEnabled(m.settings?.withdrawals_enabled!==false);setHistory(h||[]);setTonRate(t.wiener_per_ton==null?null:Number(t.wiener_per_ton));if(t.wallet?.address){setTonWallet(String(t.wallet.address));setTonProvider(String(t.wallet.provider||'TON Connect'))}}catch(e:any){if(showError||!preloaded.length)setMessage(String(e.message||e))}};
 useEffect(()=>{load(false);const ui=getTonUI();const off=ui.onStatusChange(async(w:any)=>{try{if(w?.account?.address){const address=String(w.account.address),provider=String(w.device?.appName||w.device?.name||'TON Connect');setTonWallet(address);setTonProvider(provider);await tonApi('bind',{address,provider})}}catch(e:any){setMessage(String(e.message||e))}});return()=>{try{off()}catch{}}},[]);
 useEffect(()=>{if(!cooldownUntil)return;const tick=()=>setCooldownMs(remaining(cooldownUntil));tick();const id=setInterval(tick,1000);return()=>clearInterval(id)},[cooldownUntil]);
 const walletOk=useMemo(()=>{if(!method)return false;if(isGram)return !!tonWallet&&wallet===tonWallet;const w=wallet.trim();return method.network==='Polygon'?/^0x[a-fA-F0-9]{40}$/.test(w):/^(?:[EUUk0][Qq][A-Za-z0-9_-]{46}|(?:-1|0):[a-fA-F0-9]{64})$/.test(w)},[wallet,method,isGram,tonWallet]);
 const canAmount=!!method&&globalEnabled&&method.enabled&&!hasPending&&cooldownMs<=0&&rate>0&&Number.isFinite(gross)&&gross>=Number(method.minimum_usdt)&&gross>fee&&needed<=balance+1e-9;
 const connect=async()=>{try{setTonBusy(true);await getTonUI().openModal()}catch(e:any){setMessage(String(e.message||e))}finally{setTonBusy(false)}};
 const disconnect=async()=>{try{setTonBusy(true);await getTonUI().disconnect();await tonApi('unbind');setTonWallet('');setTonProvider('');if(isGram)setWallet('')}catch(e:any){setMessage(String(e.message||e))}finally{setTonBusy(false)}};
 const choose=(m:Method)=>{if(!m.enabled||!globalEnabled)return;setMethodKey(m.method_key);setAmount('');setWallet(m.method_key==='gram_ton'?tonWallet:'');setStep('amount');setMessage('')};
 const quick=(v:'min'|'25'|'50'|'max')=>{if(!method)return;const raw=v==='min'?Number(method.minimum_usdt):v==='25'?available*.25:v==='50'?available*.5:available;setAmount((v==='min'?raw:floor4(raw)).toFixed(4))};
 const nextAmount=()=>{if(!canAmount)return;if(isGram)setWallet(tonWallet);setStep('wallet');setMessage('')};
 const submit=async()=>{if(!method||busy||!walletOk||!canAmount)return;try{setBusy(true);setMessage('');const x=await withdrawApi('request',{method_key:method.method_key,amount_usdt:gross,wallet:wallet.trim()});setBalance(Number(x.balance??balance));setHistory(p=>x.withdrawal?[x.withdrawal,...p]:p);setHasPending(true);setStep('method');setMethodKey('');setAmount('');setWallet('');setMessage('✅ Withdrawal request created. Status: Pending')}catch(e:any){setMessage(String(e.message||e).replace(/_/g,' '))}finally{setBusy(false)}};
 const idx={method:0,amount:1,wallet:2,review:3}[step];
 return <div className="wallet-v2-page"><style>{`.ton-connect-card{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:15px 16px;margin-bottom:14px}.ton-connect-copy{min-width:0}.ton-connect-copy span{font-size:10px;opacity:.5;font-weight:900;letter-spacing:.08em}.ton-connect-copy b{display:block;margin-top:4px;font-size:14px}.ton-connect-copy small{display:block;margin-top:3px;opacity:.55}.ton-connect-actions{display:flex;gap:7px}.ton-connect-actions button{height:38px;padding:0 12px;border-radius:12px;border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.06);color:#fff;font-weight:900}.ton-connect-actions .connect{background:linear-gradient(180deg,#25a8ff,#0787e8);border:0}.gram-note{margin-top:9px;padding:10px 12px;border-radius:13px;background:rgba(20,145,255,.08);border:1px solid rgba(36,158,255,.12);font-size:11px;opacity:.8}`}</style>
 <section className="ton-connect-card glass-panel"><div className="ton-connect-copy"><span>TON CONNECT</span><b>{tonWallet?short(tonWallet):'Connect TON wallet'}</b><small>{tonWallet?`${tonProvider} · GRAM withdrawals use this wallet`:'Tonkeeper · TON Space · MyTonWallet and more'}</small></div><div className="ton-connect-actions">{tonWallet?<button onClick={disconnect} disabled={tonBusy}>Disconnect</button>:<button className="connect" onClick={connect} disabled={tonBusy}>{tonBusy?'…':'CONNECT'}</button>}</div></section>
 <section className="withdraw-balance glass-panel"><span className="eyebrow">AVAILABLE BALANCE</span><div><strong>{money(balance)}</strong><b>WIENER</b></div><p>USDT rate: {baseRate.toLocaleString()} WIENER = 1 USDT{tonRate?` · TON rate: ${tonRate.toLocaleString()} WIENER = 1 GRAM`:''}</p></section>
 <section className="withdraw-v2 glass-panel"><div className="withdraw-progress">{['Select','Amount','Wallet','Review'].map((label,i)=><div className="progress-node" key={label}><span className={i<idx?'done':i===idx?'active':''}>{i<idx?'✓':i+1}</span><small>{label}</small>{i<3&&<i className={i<idx?'done':''}/>}</div>)}</div>
 {cooldownMs>0&&<div className="cooldown-state">Next withdrawal available in <b>{formatLeft(cooldownMs)}</b></div>}{hasPending&&<div className="cooldown-state">Withdrawal pending · another request unlocks after processing.</div>}
 {step==='method'&&<div className="step-panel"><div className="step-copy"><h3>Choose payout method</h3><p>USDT or native TON shown as GRAM.</p></div><div className="method-grid">{methods.map(m=>{const gram=m.method_key==='gram_ton',ready=!gram||!!tonRate;return <button key={m.method_key} onClick={()=>choose(m)} disabled={!m.enabled||!globalEnabled||!ready} className={`method-card ${!m.enabled||!globalEnabled||!ready?'disabled':''}`}><span className="method-icon"><img src={m.network==='TON'?TON_ICON:USDT_ICON} alt="" style={{width:42,height:42,objectFit:'contain',display:'block'}}/></span><div><b>{m.label}</b><small>{gram?(tonRate?`TON · Min ${Number(m.minimum_usdt).toFixed(4)} GRAM · Fee ${Number(m.fee_usdt).toFixed(4)} GRAM`:'Rate not configured yet'):`${m.network} · Min ${Number(m.minimum_usdt).toFixed(4)} USDT · 0 fee below 0.0500 USDT`}</small></div>{(!m.enabled||!ready)&&<em>Unavailable</em>}<i>›</i></button>})}</div></div>}
 {step==='amount'&&method&&<div className="step-panel"><button className="withdraw-back" onClick={()=>setStep('method')}>← Back</button><div className="network-banner"><b>{method.label}</b><span>Network: <strong>{method.network}</strong></span></div><div className="available-line"><span>Available</span><b>{money(balance)} WIENER · max {maxWithdrawable.toFixed(4)} {unit}</b></div><label>ENTER WITHDRAWAL AMOUNT</label><input className="withdraw-input" type="number" inputMode="decimal" step="0.0001" placeholder={`0.0000 ${unit}`} value={amount} onChange={e=>setAmount(e.target.value)}/><div className="quick-withdraw"><button onClick={()=>quick('min')}>MIN</button><button onClick={()=>quick('25')}>25%</button><button onClick={()=>quick('50')}>50%</button><button onClick={()=>quick('max')}>MAX</button></div><div className="withdraw-breakdown"><div><span>WIENER used</span><b>{money(Math.min(needed,balance),1)}</b></div><div><span>Fee</span><b>{fee.toFixed(4)} {unit}</b></div><div><span>You receive</span><b>{receive.toFixed(4)} {unit}</b></div></div>{!isGram&&gross>0&&gross<0.05&&<div className="cooldown-state">0 withdrawal fee below 0.0500 USDT.</div>}{gross>0&&gross<Number(method.minimum_usdt)&&<div className="withdraw-warning">Minimum is {Number(method.minimum_usdt).toFixed(4)} {unit}.</div>}{needed>balance+1e-9&&<div className="withdraw-warning">Insufficient WIENER balance.</div>}<button className="primary" disabled={!canAmount} onClick={nextAmount}>CONTINUE</button></div>}
 {step==='wallet'&&method&&<div className="step-panel"><button className="withdraw-back" onClick={()=>setStep('amount')}>← Back</button>{isGram?<><div className="network-banner"><b>Connected TON wallet</b><span>GRAM is native TON and will be sent to your TON Connect wallet.</span></div>{tonWallet?<><div className="review-withdraw"><div><span>Wallet</span><b>{short(tonWallet)}</b></div><div><span>Provider</span><b>{tonProvider||'TON Connect'}</b></div></div><button className="primary" disabled={!canAmount} onClick={()=>{setWallet(tonWallet);setStep('review')}}>USE CONNECTED WALLET</button></>:<><div className="gram-note">Connect a TON wallet before withdrawing GRAM.</div><button className="primary" onClick={connect} disabled={tonBusy}>{tonBusy?'CONNECTING…':'CONNECT TON WALLET'}</button></>}</>:<><div className="network-banner"><b>{method.label}</b><span>{method.network==='TON'?'Enter a TON-compatible wallet.':'Enter a Polygon-compatible wallet.'}</span></div><label>{method.network.toUpperCase()} WALLET ADDRESS</label><input className="withdraw-input" placeholder={method.network==='Polygon'?'0x…':'UQ… / EQ…'} value={wallet} onChange={e=>setWallet(e.target.value.trim())}/>{wallet&&!walletOk&&<div className="withdraw-warning">Invalid {method.network} wallet address.</div>}<button className="primary" disabled={!walletOk||!canAmount} onClick={()=>setStep('review')}>REVIEW WITHDRAWAL</button></>}</div>}
 {step==='review'&&method&&<div className="step-panel"><button className="withdraw-back" onClick={()=>setStep('wallet')}>← Back</button><div className="review-withdraw"><h3>Review Withdrawal</h3><div><span>Amount</span><b>{gross.toFixed(4)} {unit}</b></div><div><span>WIENER deducted</span><b>{money(needed,1)}</b></div><div><span>Network</span><b>{method.network}</b></div><div><span>Fee</span><b>{fee.toFixed(4)} {unit}</b></div><div><span>You Receive</span><b>{receive.toFixed(4)} {unit}</b></div><div><span>Wallet</span><b>{short(wallet)}</b></div></div><button className="primary confirm-withdraw" disabled={busy||!walletOk||!canAmount} onClick={submit}>{busy?'CREATING…':'CONFIRM WITHDRAWAL'}</button></div>}
 {message&&<div className="withdraw-message">{message}</div>}</section>
 <section className="withdraw-history-section"><div className="section-title"><div><h3>Withdrawal History</h3><p>Tap any row for full details.</p></div><button onClick={()=>load(true)}>↻</button></div><div className="withdraw-history-list">{history.length?history.map((w:any)=>{const st=statusView(String(w.status)),u=w.method_key==='gram_ton'?'GRAM':'USDT';return <button className="withdraw-history-row" key={w.id} onClick={()=>setSelected(w)}><div><b>{Number(w.gross_usdt).toFixed(4)} {u} · {w.network}</b><small>{date(w.created_at)}</small></div><div><strong>{Number(w.receive_usdt).toFixed(4)} {u}</strong><span>{st.icon} {st.label}</span></div></button>}):<div className="history-empty">No withdrawals yet.</div>}</div></section>
 {selected&&<div className="withdraw-sheet-backdrop" onClick={()=>setSelected(null)}><div className="withdraw-sheet" onClick={e=>e.stopPropagation()}><div className="sheet-grabber"/><div className="sheet-head"><h3>Withdrawal Details</h3><button onClick={()=>setSelected(null)}>×</button></div>{(()=>{const u=selected.method_key==='gram_ton'?'GRAM':'USDT';return [['Withdrawal ID',selected.id],['Requested Amount',`${Number(selected.gross_usdt).toFixed(4)} ${u}`],['Fee',`${Number(selected.fee_usdt).toFixed(4)} ${u}`],['Amount Received',`${Number(selected.receive_usdt).toFixed(4)} ${u}`],['Network',selected.network],['Wallet',selected.wallet_address],['Requested',date(selected.created_at)],['Status',statusView(String(selected.status)).label],['TX Hash',selected.tx_hash||'—']].map(([k,v])=><div className="sheet-row" key={String(k)}><span>{k}</span><b>{v}</b></div>)})()}{selected.explorer_url&&<button className="primary" onClick={()=>window.Telegram?.WebApp?.openLink?.(selected.explorer_url)}>OPEN BLOCKCHAIN EXPLORER</button>}</div></div>}
 </div>
}
