import {useEffect,useMemo,useState} from 'react';
import {getInitData,money,PUBLISHABLE_KEY,shareApi,SUPABASE_URL,type Snapshot} from './lib';
import {AnimatedIcon} from './icons';

type Tier={tier:number;label:string;total_wiener:number;required_ads:number;join_wiener:number;completion_wiener:number};
type Stats={invited:number;qualified:number;pending:number;earned:number};

const DEFAULT_TIERS:Tier[]=[
 {tier:1,label:'Diamond',total_wiener:600,required_ads:10,join_wiener:100,completion_wiener:500},
 {tier:2,label:'Premium',total_wiener:500,required_ads:10,join_wiener:100,completion_wiener:400},
 {tier:3,label:'Plus',total_wiener:400,required_ads:5,join_wiener:100,completion_wiener:300},
 {tier:4,label:'Base',total_wiener:300,required_ads:5,join_wiener:100,completion_wiener:200},
];
const usd=(w:number)=>`$${(Number(w||0)/20000).toFixed(3)}`;
const flag=(cc?:string|null)=>{const x=String(cc||'').toUpperCase();return /^[A-Z]{2}$/.test(x)?String.fromCodePoint(...[...x].map(c=>127397+c.charCodeAt(0))):'🌍'};

async function loadReferralV98(){
 const body=JSON.stringify({initData:getInitData()});
 const headers={'Content-Type':'application/json','apikey':PUBLISHABLE_KEY,'cache-control':'no-cache'};
 try{
  const r=await fetch(`${SUPABASE_URL}/functions/v1/wiener-referral-v2`,{method:'POST',headers,cache:'no-store',body});
  const x=await r.json();if(r.ok&&x?.ok)return{x:x.data,v2:true};
 }catch{}
 const r=await fetch(`${SUPABASE_URL}/functions/v1/wiener-referral-status`,{method:'POST',headers,cache:'no-store',body});
 const x=await r.json();if(!r.ok||!x?.ok)throw Error(x?.message||x?.error||'Unable to load referrals');
 return{x:x.data,v2:false};
}

export function Invite({data,say}:{data:Snapshot;say:any}){
 const s=data.settings,u=data.user;
 const[busy,setBusy]=useState(false),[ready,setReady]=useState(false),[refs,setRefs]=useState<any[]>([]),[tiers,setTiers]=useState<Tier[]>(DEFAULT_TIERS),[stats,setStats]=useState<Stats>({invited:Number(u.referrals_count||0),qualified:0,pending:0,earned:Number(u.referral_earnings||0)}),[security,setSecurity]=useState<any>({}),[v2,setV2]=useState(false);
 const link=`https://t.me/${String(s.bot_username||'@WienerDogeFarmBot').replace('@','')}?startapp=ref_${u.telegram_id}`;
 const shareText='🌭 Join WIENER Farm\n\n🌍 Earn rewards by watching ads, completing tasks and inviting friends.\n💰 Referral rewards depend on country and valid activity.\n\n👇 Open WIENER Farm';
 const refresh=async()=>{try{setReady(false);const r=await loadReferralV98(),x=r.x||{};setV2(r.v2);setRefs(x.referrals||[]);setTiers(Array.isArray(x.tiers)&&x.tiers.length?x.tiers:DEFAULT_TIERS);setStats(r.v2?{invited:Number(x.stats?.invited||0),qualified:Number(x.stats?.qualified||0),pending:Number(x.stats?.pending||0),earned:Number(x.stats?.earned||0)}:{invited:Number((x.referrals||[]).length),qualified:Number(x.qualified||0),pending:Math.max(0,Number((x.referrals||[]).length)-Number(x.qualified||0)),earned:Number(u.referral_earnings||0)});setSecurity(x.security||{})}catch(e:any){say(String(e?.message||'Unable to load referrals'))}finally{setReady(true)}};
 useEffect(()=>{refresh()},[]);
 useEffect(()=>{if(security?.viewer_vpn_blocked){const msg='VPN or proxy detected. Turn it off and reopen WIENER Farm to continue referral qualification.';try{(window.Telegram?.WebApp as any)?.showAlert?.(msg)}catch{}say(msg)}},[security?.viewer_vpn_blocked]);
 const fallback=()=>window.Telegram?.WebApp?.openTelegramLink?.(`https://t.me/share/url?url=${encodeURIComponent(link)}&text=${encodeURIComponent(shareText)}`);
 const share=async()=>{if(busy)return;try{setBusy(true);const wa:any=window.Telegram?.WebApp;if(typeof wa?.shareMessage!=='function'){fallback();return}const prepared=await shareApi();if(!prepared?.id)throw Error('Share message unavailable');wa.shareMessage(prepared.id)}catch(e:any){say(String(e?.message||'Unable to prepare share message'));fallback()}finally{setBusy(false)}};
 const copy=async()=>{try{await navigator.clipboard.writeText(link);say('Invite link copied')}catch{const ta=document.createElement('textarea');ta.value=link;document.body.appendChild(ta);ta.select();const ok=document.execCommand('copy');ta.remove();say(ok?'Invite link copied':'Copy failed')}};
 const sortedTiers=useMemo(()=>[...tiers].sort((a,b)=>a.tier-b.tier),[tiers]);
 const friendState=(r:any)=>{if(r.status==='banned'||r.referral_reward_eligible===false)return{label:'NOT ELIGIBLE',tone:'bad'};if(r.vpn_blocked||r.status==='vpn_blocked')return{label:'VPN OFF REQUIRED',tone:'warn'};if(r.completion_rewarded_at||r.status==='rewarded')return{label:`PAID · ${money(r.total_reward_wiener||0)}`,tone:'good'};const required=Number(r.required_ads||5),ads=Math.min(Number(r.total_ads||0),required);return{label:`${ads}/${required} ADS`,tone:'normal'}};
 return <><style>{`
 .iv{display:grid;gap:14px;padding-bottom:96px}.iv-card{border-radius:27px!important;overflow:hidden}.iv-hero{position:relative;padding:22px 18px 19px;background:radial-gradient(circle at 50% -5%,rgba(67,255,149,.2),transparent 38%),radial-gradient(circle at 96% 95%,rgba(255,198,45,.1),transparent 34%),linear-gradient(155deg,rgba(13,45,31,.92),rgba(4,20,14,.96));border:1px solid rgba(114,255,170,.13)}.iv-hero:before{content:'';position:absolute;inset:0;background:linear-gradient(120deg,transparent 30%,rgba(255,255,255,.035),transparent 68%);pointer-events:none}.iv-icon{width:72px;height:72px;margin:0 auto 13px;display:grid;place-items:center;border-radius:23px;background:linear-gradient(145deg,rgba(46,234,124,.17),rgba(255,210,65,.06));border:1px solid rgba(99,255,161,.18);box-shadow:inset 0 1px rgba(255,255,255,.08),0 15px 40px rgba(0,0,0,.3)}.iv-icon>div{width:46px;height:46px}.iv-kicker{text-align:center;font-size:9px;font-weight:950;letter-spacing:1.9px;color:#69e99f}.iv-hero h2{text-align:center;margin:7px 0 0;font-size:27px;letter-spacing:-.6px}.iv-hero p{text-align:center;margin:8px auto 0;max-width:330px;color:rgba(255,255,255,.58);font-size:12px;line-height:1.55}.iv-hero p b{color:#f5d968}.iv-pillrow{display:flex;justify-content:center;gap:7px;flex-wrap:wrap;margin-top:14px}.iv-pill{padding:7px 10px;border-radius:999px;font-size:9px;font-weight:900;background:rgba(255,255,255,.055);border:1px solid rgba(255,255,255,.07)}.iv-pill.green{color:#78eaa8;background:rgba(49,225,118,.07);border-color:rgba(76,236,137,.14)}.iv-stats{display:grid;grid-template-columns:repeat(4,1fr);gap:1px;margin-top:18px;border-radius:18px;overflow:hidden;background:rgba(255,255,255,.06)}.iv-stat{padding:12px 4px;text-align:center;background:rgba(0,0,0,.18)}.iv-stat b{display:block;font-size:17px}.iv-stat span{display:block;margin-top:4px;font-size:7px;font-weight:900;letter-spacing:.8px;opacity:.42}.iv-stat.earn b{color:#f4d456}.iv-box{padding:16px;background:linear-gradient(160deg,rgba(255,255,255,.055),rgba(255,255,255,.025));border:1px solid rgba(255,255,255,.065)}.iv-label{font-size:9px;font-weight:950;letter-spacing:1.4px;opacity:.45;margin:0 0 9px 3px}.iv-link{display:grid;grid-template-columns:minmax(0,1fr) 52px;gap:8px}.iv-linktext{height:52px;display:flex;align-items:center;padding:0 13px;border-radius:16px;background:rgba(0,0,0,.2);border:1px solid rgba(255,255,255,.055);font-size:11px;font-weight:800;opacity:.68;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.iv-copy{border:0;border-radius:16px;background:linear-gradient(180deg,#76f0a9,#36c977);color:#052214;font-size:21px;font-weight:950;box-shadow:0 8px 25px rgba(43,206,113,.16)}.iv-share{width:100%;height:54px;margin-top:9px;border-radius:17px!important;background:linear-gradient(180deg,#f8dd65,#e8bc28)!important;color:#211b00!important;font-weight:950!important}.iv-tiergrid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.iv-tier{padding:13px;border-radius:19px;background:linear-gradient(155deg,rgba(255,255,255,.05),rgba(255,255,255,.022));border:1px solid rgba(255,255,255,.065)}.iv-tier-top{display:flex;justify-content:space-between;align-items:center;gap:8px}.iv-tier-name{font-size:10px;font-weight:950}.iv-tier-total{font-size:17px;font-weight:950;color:#f3d55f}.iv-tier-usd{font-size:9px;opacity:.45}.iv-tier-rule{margin-top:10px;padding-top:9px;border-top:1px solid rgba(255,255,255,.06);font-size:9px;line-height:1.55;opacity:.6}.iv-tier-rule b{color:#74e7a4;opacity:1}.iv-rule{display:grid;grid-template-columns:34px 1fr;gap:10px;align-items:start;padding:11px 0;border-top:1px solid rgba(255,255,255,.055)}.iv-rule:first-of-type{border-top:0}.iv-rule-icon{width:34px;height:34px;display:grid;place-items:center;border-radius:12px;background:rgba(255,255,255,.045)}.iv-rule b{display:block;font-size:11px}.iv-rule span{display:block;margin-top:3px;font-size:9px;line-height:1.5;opacity:.48}.iv-vpn{padding:13px;border-radius:17px;background:rgba(255,175,44,.065);border:1px solid rgba(255,188,70,.14);font-size:10px;line-height:1.55;color:rgba(255,255,255,.7)}.iv-vpn strong{color:#ffc45f}.iv-vpn.blocked{background:rgba(255,84,84,.08);border-color:rgba(255,91,91,.18)}.iv-friends{display:grid;gap:8px}.iv-friend{padding:11px;border-radius:18px;background:rgba(255,255,255,.033);border:1px solid rgba(255,255,255,.055)}.iv-friendtop{display:grid;grid-template-columns:40px minmax(0,1fr) auto;gap:10px;align-items:center}.iv-avatar{width:40px;height:40px;display:grid;place-items:center;border-radius:50%;background:linear-gradient(145deg,rgba(70,229,133,.16),rgba(255,210,60,.09));border:1px solid rgba(87,235,145,.13);font-weight:950;color:#7beaac}.iv-name{min-width:0}.iv-name b{display:block;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.iv-name span{display:block;margin-top:2px;font-size:8px;opacity:.42}.iv-status{font-size:9px;font-weight:950}.iv-status.good{color:#70e8a1}.iv-status.warn{color:#ffc45f}.iv-status.bad{color:#ff8787}.iv-progress{height:5px;margin-top:10px;border-radius:999px;background:rgba(255,255,255,.07);overflow:hidden}.iv-progress>i{display:block;height:100%;border-radius:inherit;background:linear-gradient(90deg,#35cc79,#e8c64d)}.iv-friendmeta{display:flex;justify-content:space-between;gap:8px;margin-top:7px;font-size:8px;opacity:.46}.iv-empty{text-align:center;padding:20px 10px;font-size:10px;opacity:.45}.iv-loading{height:62px;border-radius:17px;background:linear-gradient(90deg,rgba(255,255,255,.025),rgba(255,255,255,.07),rgba(255,255,255,.025));background-size:220% 100%;animation:ivsh 1.15s linear infinite}@keyframes ivsh{to{background-position:-220% 0}}@media(max-width:340px){.iv-tiergrid{grid-template-columns:1fr}.iv-stats{grid-template-columns:repeat(2,1fr)}}
 `}</style><div className="iv">
 <section className="card iv-card iv-hero"><div className="iv-icon"><AnimatedIcon name="invite" active/></div><div className="iv-kicker">COUNTRY REWARDS</div><h2>Invite & Earn</h2><p>Earn <b>$0.015–$0.03</b> for each valid friend. Every qualified referral starts with <b>100 WIENER</b>, then unlocks the rest after verified ads.</p><div className="iv-pillrow"><span className="iv-pill green">+100 WIENER on valid join</span><span className="iv-pill">5–10 verified ads</span></div><div className="iv-stats"><div className="iv-stat"><b>{ready?stats.invited:'—'}</b><span>INVITED</span></div><div className="iv-stat"><b>{ready?stats.qualified:'—'}</b><span>QUALIFIED</span></div><div className="iv-stat"><b>{ready?stats.pending:'—'}</b><span>PENDING</span></div><div className="iv-stat earn"><b>{ready?money(stats.earned):'—'}</b><span>V2 EARNED</span></div></div></section>
 <section className="card iv-card iv-box"><div className="iv-label">YOUR PERSONAL LINK</div><div className="iv-link"><div className="iv-linktext">{link}</div><button className="iv-copy" onClick={copy}>⧉</button></div><button className="primary iv-share" disabled={busy} onClick={share}>{busy?'PREPARING…':'➤  SHARE WITH FRIENDS'}</button></section>
 <section className="card iv-card iv-box"><div className="iv-label">REWARD PLAN</div><div className="iv-tiergrid">{sortedTiers.map(t=><div className="iv-tier" key={t.tier}><div className="iv-tier-top"><div><div className="iv-tier-name">{t.tier===1?'💎':t.tier===2?'🥈':t.tier===3?'🥉':'🌍'} Tier {t.tier}</div><div className="iv-tier-usd">{usd(t.total_wiener)} total</div></div><div className="iv-tier-total">{money(t.total_wiener)}</div></div><div className="iv-tier-rule"><b>100</b> on verified join · <b>+{money(t.completion_wiener)}</b> after {t.required_ads} valid ads</div></div>)}</div></section>
 <section className="card iv-card iv-box"><div className="iv-label">HOW A REFERRAL QUALIFIES</div><div className="iv-rule"><div className="iv-rule-icon">🌍</div><div><b>Country is detected automatically</b><span>The user cannot choose a tier. Their country and reward tier are frozen by the server for the referral.</span></div></div><div className="iv-rule"><div className="iv-rule-icon">✅</div><div><b>Only valid credited ads count</b><span>Tier 1–2 require 10 ads. Tier 3–4 require 5 ads. Failed or rejected activity does not count.</span></div></div><div className="iv-rule"><div className="iv-rule-icon">🛡️</div><div><b>One device = one account</b><span>Multi-account and self-referral abuse are not eligible and can cause an account restriction.</span></div></div><div className={`iv-vpn ${security?.viewer_vpn_blocked?'blocked':''}`}><strong>{security?.viewer_vpn_blocked?'VPN / proxy detected':'VPN / proxy rule'}</strong><br/>{security?.viewer_vpn_blocked?'Turn it off and reopen WIENER Farm. Referral qualification is paused while it is detected.':'If a VPN or proxy is detected, referral rewards are paused. Turn it off and reopen the app.'}</div></section>
 <section className="card iv-card iv-box"><div className="iv-label">YOUR FRIENDS · {refs.length}</div>{!ready?<><div className="iv-loading"/><div style={{height:8}}/><div className="iv-loading"/></>:refs.length?<div className="iv-friends">{refs.slice(0,20).map(r=>{const st=friendState(r),name=r.first_name||r.username||`User ${r.telegram_id}`,required=Number(r.required_ads||5),ads=Math.min(Number(r.total_ads||0),required),pct=Math.max(0,Math.min(100,required?ads/required*100:0));return <div className="iv-friend" key={r.telegram_id}><div className="iv-friendtop"><div className="iv-avatar">{String(name)[0]?.toUpperCase()||'?'}</div><div className="iv-name"><b>{name}</b><span>{flag(r.country_code)} {r.country_code||'Country pending'} · {r.tier?`Tier ${r.tier}`:'Legacy referral'}</span></div><div className={`iv-status ${st.tone}`}>{st.label}</div></div><div className="iv-progress"><i style={{width:`${pct}%`}}/></div><div className="iv-friendmeta"><span>{ads}/{required} valid ads</span><span>{r.total_reward_wiener?`${money(r.total_reward_wiener)} WIENER total`:v2?'Tier pending':'Existing referral'}</span></div></div>})}</div>:<div className="iv-empty">No referrals yet. Share your link to start earning.</div>}</section>
 </div></>
}
