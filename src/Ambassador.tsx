import {useEffect,useState} from 'react';
import {ambassadorApi,type Snapshot,type Tab} from './lib';

const usd=(v:any,d=4)=>Number(v||0).toFixed(d);
const cleanErr=(v:any)=>String(v?.message||v||'Request failed').replace(/^telegram:/,'').replace(/_/g,' ');

export function AmbassadorIcon({size=44,active=true}:{size?:number;active?:boolean}){
  return <span className={`amb-icon ${active?'active':''}`} style={{width:size,height:size}} aria-hidden="true"><span className="amb-medal"><b>W</b></span><i/><em/></span>
}

export function AmbassadorHomeCard({setTab}:{setTab:(t:Tab)=>void}){
  return <section className="card ambassador-home-card" onClick={()=>setTab('ambassador')} role="button" tabIndex={0}>
    <div className="amb-home-icon"><AmbassadorIcon size={54}/></div>
    <div className="grow"><div className="eyebrow">WIENER PARTNERS</div><h3>Ambassador Program</h3><p>Own a 1,000+ subscriber channel? Earn direct USDT from valid promo claims.</p></div>
    <button className="amb-open" onClick={e=>{e.stopPropagation();setTab('ambassador')}}>OPEN</button>
    <style>{ambCss}</style>
  </section>
}

type O={settings:any;ambassador:any;promos:any[];withdrawals:any[];week_claims:number;weekly_rank:number|null;promos_today:number;leaderboard:any[]};

export function AmbassadorPage({data,setTab,say}:{data:Snapshot;setTab:(t:Tab)=>void;say:(s:string)=>void}){
  const [o,setO]=useState<O|null>(null),[busy,setBusy]=useState(false),[channel,setChannel]=useState(''),[claims,setClaims]=useState(50),[wallet,setWallet]=useState(''),[amount,setAmount]=useState('0.10'),[admin,setAdmin]=useState<any>(null),[adminBusy,setAdminBusy]=useState(false);
  const load=async()=>{try{setO(await ambassadorApi('overview'))}catch(e:any){say(cleanErr(e))}};
  const loadAdmin=async()=>{if(!data.is_admin)return;try{setAdmin(await ambassadorApi('admin_boot'))}catch(e:any){say(cleanErr(e))}};
  useEffect(()=>{load();if(data.is_admin)loadAdmin()},[]);
  const run=async(fn:()=>Promise<any>,ok:string)=>{if(busy)return;try{setBusy(true);await fn();say(ok);await load();if(data.is_admin)await loadAdmin()}catch(e:any){say(cleanErr(e))}finally{setBusy(false)}};
  const a=o?.ambassador,c=o?.settings;
  const active=!!a&&['approved','active'].includes(a.status);
  const pending=a?.status==='pending',rejected=a?.status==='rejected',suspended=a?.status==='suspended';
  const addBot=()=>window.Telegram?.WebApp?.openTelegramLink?.('https://t.me/WienerDogeFarmBot?startchannel&admin=post_messages+edit_messages+delete_messages');
  return <div className="amb-page"><style>{ambCss}</style>
    <div className="amb-head"><button className="amb-back" onClick={()=>setTab('home')}>‹</button><div><div className="eyebrow">WIENER FARM</div><h2>Ambassador Program</h2></div><AmbassadorIcon size={48}/></div>
    {!o?<section className="card amb-center">Loading Ambassador Program…</section>:<>
      {!active&&<section className="card amb-hero"><AmbassadorIcon size={82}/><div className="amb-status">{pending?'APPLICATION PENDING':rejected?'APPLICATION REJECTED':suspended?'ACCOUNT SUSPENDED':'BECOME AN AMBASSADOR'}</div><h1>Grow WIENER.<br/>Earn USDT.</h1><p>Promote verified WIENER promo drops through your Telegram channel and earn <b>${usd(c.commission_per_valid_claim_usdt,4)} USDT</b> for every valid claim.</p><div className="amb-benefits"><span>📣 {c.promos_per_day} promos/day</span><span>💵 Direct USDT</span><span>🏆 ${usd(c.weekly_prize_pool_usdt,0)} weekly pool</span><span>✅ Verified badge</span></div></section>}

      {!active&&!pending&&<>
        <section className="card amb-step"><div className="amb-step-no">1</div><div className="grow"><h3>Connect Your Channel</h3><p>Public Telegram channel with at least <b>{Number(c.min_subscribers).toLocaleString()} subscribers</b>.</p></div></section>
        <section className="card amb-connect"><button className="secondary" onClick={addBot}>+ ADD WIENER BOT TO CHANNEL</button><div className="amb-divider"><span>then verify</span></div><input value={channel} onChange={e=>setChannel(e.target.value)} placeholder="@yourchannel or t.me/yourchannel"/><button className="primary" disabled={busy||!channel.trim()} onClick={()=>run(()=>ambassadorApi('verify_channel',{channel:channel.trim()}),'Channel verified')}>{busy?'VERIFYING…':'VERIFY CHANNEL'}</button><p className="tiny center">We verify channel existence, public username, your admin/owner role, bot permissions and subscriber count directly through Telegram.</p></section>
        {a?.channel_id&&<section className="card amb-verified"><div className="amb-check">✓</div><div className="grow"><h3>{a.channel_title}</h3><p>{a.channel_username} · {Number(a.subscriber_count).toLocaleString()} subscribers</p></div><span className="amb-pill good">VERIFIED</span></section>}
        {a?.channel_id&&<section className="card amb-eligibility"><h3>Eligibility Check</h3><Check ok={!!a.channel_id} text="Channel exists"/><Check ok={!!a.channel_username} text="Public username available"/><Check ok={Number(a.subscriber_count)>=Number(c.min_subscribers)} text={`${Number(c.min_subscribers).toLocaleString()}+ subscribers`}/><Check ok={['creator','administrator'].includes(a.applicant_role)} text="You are owner/admin"/><Check ok={a.bot_is_admin&&a.bot_can_post} text="WIENER bot admin + post access"/><button className="primary" disabled={busy} onClick={()=>run(()=>ambassadorApi('apply'),'Application submitted')}>APPLY AS AMBASSADOR</button></section>}
      </>}

      {pending&&<section className="card amb-pending"><div className="amb-clock">◷</div><h3>Application under review</h3><p>{a.channel_username} · {Number(a.subscriber_count).toLocaleString()} subscribers</p><p className="muted">Your verified channel details are with the WIENER admin team. You can keep using the app normally.</p><button className="secondary" onClick={()=>run(()=>ambassadorApi('refresh_channel'),'Channel refreshed')}>REFRESH CHANNEL CHECK</button></section>}
      {rejected&&<section className="card amb-alert"><b>Application rejected</b><p>{a.rejection_reason||'Your application did not meet the current program requirements.'}</p><button className="secondary" onClick={()=>run(()=>ambassadorApi('refresh_channel'),'Channel rechecked')}>RECHECK CHANNEL</button></section>}
      {suspended&&<section className="card amb-alert"><b>Ambassador access suspended</b><p>New promo creation and withdrawals are paused. Contact @WienerSupport if you need a review.</p></section>}

      {active&&<>
        <section className="card amb-dashboard-hero"><div className="amb-verified-title"><AmbassadorIcon size={60}/><div><span className="amb-pill good">VERIFIED AMBASSADOR</span><h2>{a.channel_title}</h2><p>{a.channel_username} · {Number(a.subscriber_count).toLocaleString()} subscribers</p></div></div><div className="amb-balance"><small>AMBASSADOR BALANCE</small><strong>${usd(a.balance_usdt,4)}</strong><span>USDT · Polygon</span></div><div className="amb-stats"><div><b>{o.promos_today}/{c.promos_per_day}</b><span>Promos today</span></div><div><b>{o.week_claims}</b><span>Weekly claims</span></div><div><b>{o.weekly_rank?`#${o.weekly_rank}`:'—'}</b><span>Weekly rank</span></div></div></section>

        <section className="card amb-create"><div className="section-head"><div><h3>Create Promo Drop</h3><p>Bot posts it directly to your verified channel.</p></div><span className="reward-chip">+${usd(c.commission_per_valid_claim_usdt,4)}/claim</span></div><div className="amb-package-row">{[25,50,100].map(n=><button key={n} className={claims===n?'active':''} onClick={()=>setClaims(n)}>{n}<small>claims</small></button>)}</div><div className="amb-preview"><span>Reward/user</span><b>{c.promo_reward_wiener} WIENER</b><span>Your max commission</span><b>${usd(claims*Number(c.commission_per_valid_claim_usdt),4)}</b></div><button className="primary" disabled={busy||o.promos_today>=c.promos_per_day} onClick={()=>run(()=>ambassadorApi('create_promo',{max_claims:claims}),'Promo posted to your channel')}>{o.promos_today>=c.promos_per_day?'DAILY PROMO LIMIT REACHED':busy?'CREATING…':'CREATE & POST PROMO'}</button></section>

        <section className="card amb-withdraw"><div className="section-head"><div><h3>Withdraw USDT</h3><p>Direct Polygon USDT · Min ${usd(c.min_withdraw_usdt,2)}</p></div></div><input value={wallet} onChange={e=>setWallet(e.target.value.trim())} placeholder="0x… Polygon wallet"/><div className="amb-amount"><input inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="0.10"/><button onClick={()=>setAmount(String(a.balance_usdt))}>MAX</button></div><button className="primary" disabled={busy||Number(amount)<Number(c.min_withdraw_usdt)||Number(amount)>Number(a.balance_usdt)||!/^0x[0-9a-fA-F]{40}$/.test(wallet)} onClick={()=>run(()=>ambassadorApi('withdraw',{amount:Number(amount),wallet}),'Withdrawal requested')}>WITHDRAW USDT</button></section>

        <section className="card"><div className="section-head"><div><h3>My Promos</h3><p>Valid claims generate USDT automatically.</p></div></div>{o.promos.length?o.promos.slice(0,8).map(p=><div className="amb-row" key={p.id}><div><b>{p.code}</b><span>{p.valid_claims}/{p.max_claims} valid claims</span></div><div className="right"><b>${usd(Number(p.valid_claims)*Number(p.commission_per_claim_usdt),4)}</b><span className={`amb-pill ${p.status==='active'?'good':''}`}>{p.status}</span></div></div>):<div className="empty">No ambassador promos yet.</div>}</section>

        <section className="card"><div className="section-head"><div><h3>Withdrawal History</h3><p>Separate from normal WIENER withdrawals.</p></div></div>{o.withdrawals.length?o.withdrawals.slice(0,8).map(w=><div className="amb-row" key={w.id}><div><b>{w.public_id}</b><span>{w.network} · {w.status}</span></div><div className="right"><b>${usd(w.amount_usdt,4)}</b>{w.tx_hash&&<button className="amb-link" onClick={()=>window.Telegram?.WebApp?.openLink?.(`https://polygonscan.com/tx/${w.tx_hash}`)}>TX</button>}</div></div>):<div className="empty">No ambassador withdrawals yet.</div>}</section>
      </>}

      <section className="card amb-league"><div className="section-head"><div><h3>🏆 Weekly Ambassador League</h3><p>${usd(c.weekly_prize_pool_usdt,0)} USDT prize pool · ranked by valid claims</p></div></div>{o.leaderboard.length?o.leaderboard.map(x=><div className="amb-row" key={x.id}><div><b>#{x.rank} {x.channel_username||x.channel_title||'Ambassador'}</b></div><div className="right"><b>{x.claims}</b><span>claims</span></div></div>):<div className="empty">Leaderboard starts when valid claims arrive.</div>}</section>

      {data.is_admin&&<AdminAmbassador data={admin} busy={adminBusy} setBusy={setAdminBusy} reload={loadAdmin} say={say}/>}    
    </>}
  </div>
}

function Check({ok,text}:{ok:boolean;text:string}){return <div className="amb-check-row"><i className={ok?'ok':''}>{ok?'✓':'!'}</i><span>{text}</span></div>}

function AdminAmbassador({data,busy,setBusy,reload,say}:{data:any;busy:boolean;setBusy:any;reload:any;say:any}){
 const action=async(fn:()=>Promise<any>,ok:string)=>{if(busy)return;try{setBusy(true);await fn();say(ok);await reload()}catch(e:any){say(cleanErr(e))}finally{setBusy(false)}};
 const [tx,setTx]=useState<Record<string,string>>({});
 if(!data)return <section className="card amb-admin"><h3>Ambassador Admin</h3><p>Loading…</p></section>;
 return <section className="card amb-admin"><div className="section-head"><div><div className="eyebrow">ADMIN</div><h3>Ambassador Management</h3><p>Applications and direct USDT withdrawals.</p></div></div><h4>Applications</h4>{data.applications?.filter((a:any)=>['pending','active','suspended','rejected'].includes(a.status)).slice(0,20).map((a:any)=><div className="amb-admin-item" key={a.id}><div><b>{a.channel_username||'No channel'}</b><span>UID {a.telegram_id} · {Number(a.subscriber_count).toLocaleString()} subs · {a.status}</span></div><div className="amb-admin-actions">{a.status==='pending'&&<><button disabled={busy} onClick={()=>action(()=>ambassadorApi('admin_status',{ambassador_id:a.id,status:'active'}),'Ambassador approved')}>APPROVE</button><button disabled={busy} onClick={()=>action(()=>ambassadorApi('admin_status',{ambassador_id:a.id,status:'rejected',reason:'Application did not meet program requirements'}),'Application rejected')}>REJECT</button></>}{a.status==='active'&&<button disabled={busy} onClick={()=>action(()=>ambassadorApi('admin_status',{ambassador_id:a.id,status:'suspended'}),'Ambassador suspended')}>SUSPEND</button>}{a.status==='suspended'&&<button disabled={busy} onClick={()=>action(()=>ambassadorApi('admin_status',{ambassador_id:a.id,status:'active'}),'Ambassador restored')}>RESTORE</button>}</div></div>)}<h4>Pending USDT Withdrawals</h4>{data.withdrawals?.filter((w:any)=>w.status==='pending').map((w:any)=><div className="amb-admin-item" key={w.id}><div><b>${usd(w.amount_usdt,4)} · {w.public_id}</b><span>{w.wallet_address}</span><input value={tx[w.id]||''} onChange={e=>setTx(x=>({...x,[w.id]:e.target.value.trim()}))} placeholder="Polygon TX hash after payment"/></div><div className="amb-admin-actions"><button disabled={busy||!/^0x[0-9a-fA-F]{64}$/.test(tx[w.id]||'')} onClick={()=>action(()=>ambassadorApi('admin_withdraw_paid',{withdrawal_id:w.id,tx_hash:tx[w.id]}),'Withdrawal marked paid')}>PAID</button><button disabled={busy} onClick={()=>action(()=>ambassadorApi('admin_withdraw_reject',{withdrawal_id:w.id,reason:'Withdrawal rejected by admin'}),'Withdrawal rejected & refunded')}>REJECT</button></div></div>)}</section>
}

const ambCss=`
.amb-icon{position:relative;display:inline-grid;place-items:center;flex:0 0 auto;filter:drop-shadow(0 10px 20px rgba(255,190,0,.18))}.amb-medal{position:absolute;inset:4% 8% 20%;display:grid;place-items:center;border-radius:50%;background:radial-gradient(circle at 32% 25%,#fff7a6 0 8%,#ffd84a 24%,#d99a00 72%,#795000);border:2px solid rgba(255,245,171,.72);box-shadow:inset 0 2px 5px rgba(255,255,255,.65),inset 0 -5px 9px rgba(92,52,0,.35),0 8px 18px rgba(0,0,0,.22)}.amb-medal:after{content:'';position:absolute;inset:18%;border-radius:50%;background:radial-gradient(circle at 36% 28%,#8effa6,#23a858 58%,#075b2b);border:1px solid rgba(255,255,255,.45);box-shadow:inset 0 1px 3px rgba(255,255,255,.5)}.amb-medal b{z-index:1;color:#fff;font-size:.42em;font-weight:1000;text-shadow:0 2px 4px rgba(0,0,0,.35)}.amb-icon i,.amb-icon em{position:absolute;bottom:2%;width:26%;height:34%;background:linear-gradient(180deg,#f5b90d,#9f6500);clip-path:polygon(0 0,100% 0,82% 100%,50% 72%,18% 100%)}.amb-icon i{left:23%;transform:rotate(8deg)}.amb-icon em{right:23%;transform:rotate(-8deg)}.amb-icon.active:before{content:'';position:absolute;z-index:4;top:10%;left:22%;width:18%;height:38%;border-radius:50%;background:linear-gradient(110deg,transparent,rgba(255,255,255,.85),transparent);transform:rotate(25deg);opacity:.65;animation:ambShine 3.8s ease-in-out infinite}@keyframes ambShine{0%,75%,100%{transform:translateX(-8px) rotate(25deg);opacity:0}84%{opacity:.75}92%{transform:translateX(24px) rotate(25deg);opacity:0}}
.ambassador-home-card{display:flex;align-items:center;gap:13px;cursor:pointer;position:relative;overflow:hidden;border-color:rgba(255,205,70,.16)!important;background:linear-gradient(135deg,rgba(255,195,30,.08),rgba(24,174,88,.06))!important}.ambassador-home-card:after{content:'';position:absolute;width:120px;height:120px;right:-55px;top:-55px;border-radius:50%;background:rgba(255,194,26,.06);filter:blur(2px)}.ambassador-home-card h3{margin:2px 0 4px}.ambassador-home-card p{margin:0;font-size:12px;line-height:1.45;opacity:.62}.amb-home-icon{display:grid;place-items:center}.amb-open{border:0;border-radius:12px;padding:9px 10px;font-weight:900;font-size:10px;background:rgba(255,210,51,.12);color:#ffd84e;border:1px solid rgba(255,210,51,.16)}
.amb-page{display:flex;flex-direction:column;gap:16px;padding-bottom:96px}.amb-head{display:flex;align-items:center;gap:12px}.amb-head>div:nth-child(2){flex:1}.amb-head h2{margin:1px 0;font-size:24px}.amb-back{width:38px;height:38px;border-radius:12px;border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.05);color:#fff;font-size:28px;line-height:1}.amb-center{text-align:center}.amb-hero{text-align:center;padding:25px 20px!important;background:radial-gradient(circle at 50% 0,rgba(255,198,20,.15),transparent 38%),linear-gradient(145deg,rgba(25,157,81,.09),rgba(255,255,255,.03))!important}.amb-status{margin-top:7px;font-size:10px;font-weight:1000;letter-spacing:1.5px;color:#ffd74c}.amb-hero h1{margin:9px 0;font-size:31px;line-height:1.02}.amb-hero p{max-width:330px;margin:0 auto;opacity:.68;font-size:13px;line-height:1.55}.amb-benefits{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:18px}.amb-benefits span{padding:10px;border-radius:13px;background:rgba(255,255,255,.045);font-size:10px;font-weight:800}.amb-step{display:flex;align-items:center;gap:12px}.amb-step-no{width:34px;height:34px;display:grid;place-items:center;border-radius:50%;background:rgba(255,206,45,.13);color:#ffd43d;font-weight:1000}.amb-step h3,.amb-step p{margin:0}.amb-step p{font-size:12px;opacity:.58;margin-top:3px}.amb-connect{display:flex;flex-direction:column;gap:10px}.amb-connect input,.amb-withdraw input,.amb-admin-item input{width:100%;box-sizing:border-box;border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.045);color:#fff;padding:13px 14px;border-radius:14px;outline:0}.amb-divider{text-align:center;font-size:9px;opacity:.38;text-transform:uppercase;letter-spacing:1px}.amb-verified{display:flex;align-items:center;gap:12px}.amb-check{width:38px;height:38px;display:grid;place-items:center;border-radius:50%;background:rgba(47,210,105,.13);color:#62f89a;font-weight:1000}.amb-verified h3,.amb-verified p{margin:0}.amb-verified p{font-size:11px;opacity:.6;margin-top:3px}.amb-pill{display:inline-flex;padding:5px 8px;border-radius:999px;font-size:8px;font-style:normal;font-weight:1000;text-transform:uppercase;background:rgba(255,255,255,.06);opacity:.75}.amb-pill.good{background:rgba(45,214,108,.12);color:#6aff9e;opacity:1}.amb-eligibility h3{margin-top:0}.amb-check-row{display:flex;align-items:center;gap:9px;padding:8px 0;font-size:12px}.amb-check-row i{width:22px;height:22px;display:grid;place-items:center;border-radius:50%;background:rgba(255,86,86,.1);color:#ff7777;font-style:normal}.amb-check-row i.ok{background:rgba(45,214,108,.12);color:#63f998}.amb-eligibility .primary{margin-top:12px;width:100%}.amb-pending,.amb-alert{text-align:center}.amb-clock{font-size:34px}.amb-pending h3{margin:7px 0}.amb-pending p{font-size:12px}.amb-alert{border-color:rgba(255,118,70,.2)!important}.amb-dashboard-hero{background:radial-gradient(circle at 82% 0,rgba(255,198,20,.15),transparent 34%),linear-gradient(145deg,rgba(18,142,70,.12),rgba(255,255,255,.025))!important}.amb-verified-title{display:flex;align-items:center;gap:13px}.amb-verified-title h2{margin:5px 0 2px;font-size:20px}.amb-verified-title p{margin:0;font-size:11px;opacity:.55}.amb-balance{text-align:center;padding:25px 0 18px}.amb-balance small{display:block;font-size:9px;letter-spacing:1.3px;opacity:.48}.amb-balance strong{display:block;font-size:37px;margin:5px 0 1px}.amb-balance span{font-size:10px;color:#6af49a}.amb-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.amb-stats div{text-align:center;padding:11px 6px;border-radius:14px;background:rgba(255,255,255,.045)}.amb-stats b,.amb-stats span{display:block}.amb-stats b{font-size:16px}.amb-stats span{font-size:8px;opacity:.47;margin-top:3px}.amb-create .section-head,.amb-withdraw .section-head{margin-bottom:12px}.amb-package-row{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.amb-package-row button{border:1px solid rgba(255,255,255,.08);border-radius:14px;background:rgba(255,255,255,.035);color:#fff;padding:12px 6px;font-weight:1000}.amb-package-row button.active{background:rgba(255,206,45,.12);border-color:rgba(255,206,45,.32);color:#ffe063}.amb-package-row small{display:block;font-size:8px;opacity:.55;margin-top:2px}.amb-preview{display:grid;grid-template-columns:1fr auto;gap:7px 10px;margin:13px 0;padding:12px;border-radius:14px;background:rgba(255,255,255,.035);font-size:11px}.amb-preview span{opacity:.5}.amb-withdraw{display:flex;flex-direction:column;gap:9px}.amb-amount{display:flex;gap:8px}.amb-amount input{flex:1}.amb-amount button,.amb-link{border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.05);color:#fff;border-radius:12px;padding:0 13px;font-weight:900}.amb-row{display:flex;justify-content:space-between;gap:10px;padding:12px 0;border-top:1px solid rgba(255,255,255,.055)}.amb-row:first-of-type{border-top:0}.amb-row b,.amb-row span{display:block}.amb-row span{font-size:9px;opacity:.48;margin-top:3px}.amb-row .right{text-align:right}.amb-league{border-color:rgba(255,206,45,.15)!important}.amb-admin{border-color:rgba(255,90,90,.18)!important}.amb-admin h4{font-size:11px;text-transform:uppercase;letter-spacing:1px;opacity:.55;margin:18px 0 8px}.amb-admin-item{padding:12px 0;border-top:1px solid rgba(255,255,255,.06);display:flex;gap:10px;justify-content:space-between;align-items:flex-start}.amb-admin-item>div:first-child{min-width:0;flex:1}.amb-admin-item b,.amb-admin-item span{display:block}.amb-admin-item span{font-size:9px;opacity:.5;margin:4px 0;word-break:break-all}.amb-admin-actions{display:flex;gap:5px;flex-wrap:wrap;justify-content:flex-end}.amb-admin-actions button{border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.05);color:#fff;border-radius:9px;padding:7px 8px;font-size:8px;font-weight:900}.secondary{width:100%;border:1px solid rgba(255,255,255,.09);background:rgba(255,255,255,.045);color:#fff;border-radius:14px;padding:13px;font-weight:900}
`;
